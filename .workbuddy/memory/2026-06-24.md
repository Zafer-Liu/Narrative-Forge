# 2026-06-24 工作日志

## Narrative Forge 项目优化方案撰写
- 审阅了项目代码：app.py(1131行)、static/app.js(2733行/146KB 裸全局脚本)、backend/(publishing.py/serial_exporting.py/route_registry.py/test_server.py 674行)、static/index.html。
- 关键发现：
  - 前端 app.js 巨石单文件、全局可变 `project`、38处 innerHTML 全量重绘（XSS+性能痛点），无构建/打包。
  - 导出（serial_exporting.py::_concat_episode）FFmpeg 同步阻塞，无进度/取消。
  - 后端测试扎实但前端零测试；FFmpeg 真实产物被 mock。
  - static/samples 占 166MB（48个mp4）随仓库提交。
- 产出：`项目优化方案.md`（四阶段：异步化+XSS止血 → 前端ES Module化+esbuild+测试 → 后端分层 → 适配器插件化）。建议坚持"无重框架"，用 esbuild + 原生 ESM。

## 前端 P0 优化：app.js 模块化（已完成）
- 决策（用户确认）：一次性完整拆分 app.js + 引入 esbuild + Vitest。
- 把 2733 行裸全局 `static/app.js` 拆成 `static/src/` 下 16 个 ES Module：state(显式store/常量/DOM缓存) / utils(纯函数+escapeHtml) / project-model / prompt / episodes / mode / draft / api / story-graph / scene-list / render(hub) / tree / player / project-io / events / episode-model(ESM版,仍挂 window.FrameForgeEpisodeModel) / main(入口)。
- 关键手法：跨模块可重新赋值的全局（project/currentMode/playerState/serialState/treeZoom 等）→ state.js 用 `export let + setXxx()`（ESM 实时绑定，读取不变，仅约12处重新赋值改成 setter）。初始 project 创建从解析期移到 main.js 的 bootstrap()（DOMContentLoaded 后）。
- 构建：`build.mjs`(esbuild→static/dist/bundle.js, IIFE, minify, sourcemap, 146KB→102.6KB)。index.html 改为单 `<script src="/dist/bundle.js?v=26">`。旧 4 个散文件移到 `.backup-legacy-frontend/`（非 git 仓库，未删除）。
- 测试：Vitest+jsdom，`tests/` 下 utils(22)/episode-model(8)/bundle-smoke(5，真实 index.html 注入 jsdom 跑 bundle 验证启动) = 35 通过；后端 47 通过（更新了 test_serves_workbench_assets 改测 /dist/bundle.js）。
- 环境：Python venv `~/.workbuddy/binaries/python/envs/default`（已装 requests）；node 用 `/c/Program Files/nodejs`。
- 待办（下一阶段）：导出任务异步化（P0）、后端 app.py 分层。

## 多供应商 + 测试连接（已完成）
- 用户需求：文生图/图生视频都加「测试连接」按钮，并支持更多家模型。确认：图像新增 OpenAI 兼容 + 通义万相；视频新增火山方舟 Seedance + 通义万相；测试连接=仅校验连通+鉴权（零成本）。
- 后端新增 `backend/providers.py` 适配器层：统一契约 submit→{data:{id}}、poll→{data:{status,outputs}}。适配器：AtlasCloudImage/Video、OpenAIImage(同步 /images/generations)、DashScopeImage/Video(异步 X-DashScope-Async + /tasks/{id})、SeedanceVideo(火山方舟 /contents/generations/tasks)。`get_adapter(kind,name)`/`list_providers()`。
- app.py：generate_image/video 与 api_prediction 改为按 provider 走适配器并归一化；同步供应商用 SYNC_RESULTS 缓存（id 以 sync- 开头，轮询回放）；新增 provider_probe()（401/403=鉴权失败，其余可达HTTP=通过）；新增端点 /api/providers、/api/test-image-provider、/api/test-video-provider。旧 build_image_payload/build_video_payload 改为委托 AtlasCloud 适配器，保持旧测试契约不变。atlas_request 加 extra_headers 参数。
- 前端：index.html 图像/视频设置区加供应商下拉+测试按钮；state DOM 缓存、project-model meta(imageProvider/videoProvider)+providerSettings 带 provider、api.js 生成请求带 *_provider、轮询带 X-Provider-Name、draft.js 加 testImageProvider/testVideoProvider、events.js 绑定按钮+bindProviderSwitch(切换供应商自动填默认 baseURL/model，默认值取自 /api/providers)。bundle v27。
- 协议要点：Seedance=火山方舟 ark.cn-beijing.volces.com/api/v3，POST contents/generations/tasks→{id}，GET .../{id}→{status,content:{video_url}}。DashScope=dashscope.aliyuncs.com，需 header X-DashScope-Async:enable，提交→{output:{task_id}}，GET api/v1/tasks/{id}→{output:{task_status,results/video_url}}。
- 测试：后端 47→61 全绿（新增 ProviderAdapterTests/ProviderProbeTests + 3 集成端点测试）；前端 35 全绿；真实服务器验证 /api/providers 与页面下拉/按钮 OK。
