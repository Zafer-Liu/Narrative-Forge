{
  "name": "narrative-forge",
  "version": "1.0.0",
  "private": true,
  "description": "Narrative Forge frontend build & test tooling (dev-only; runtime stays zero-dependency static files)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "vitest": "^2.0.0",
    "jsdom": "^24.1.0"
  }
}
